# dependencies
node_modules/

# local build toolchain (JDK / bun used for local builds)
.toolchain/

# JetBrains plugin build output
hosts/jetbrains-plugin/build/
hosts/jetbrains-plugin/.gradle/
hosts/jetbrains-plugin/.intellijPlatform/
hosts/jetbrains-plugin/.kotlin/

# web UI build output (bundled into the plugin at build time)
packages/opencode/webgui-dist/
packages/opencode/webgui/dist/

# packaged extensions
*.vsix
opencode-plugin-*.zip

# misc
*.log
.DS_Store
.env
.env.local
